# 开箱与第一次使用

## 给裸板供电

板子使用单节锂电池输入。14500 只是外形规格，必须确认收到的是适配本板的单节锂离子电池，而不是其他化学体系。电池正极接 J2 的 BAT，负极接 GND，按 PCB 网络/丝印核对，不仅凭插头塑料方向判断。SW1 打开后供电，首次放平约两秒完成静止校准。

**Type-C 是数据接口，不承担本板主电源供电；板上没有充电电路。** 插电脑时仍需电池或合适的外部电源给 J2 供电。电池用适配的外部充电器充电。裸板不要直接放在金属表面，电池和板子应固定，避免移动时短路。

原开发调试电源为 3.7 V；首次检查可以限流上电。正常 3V3 电源约为 3.3 V。没有异常发热、能校准并在网页看到正常状态后再录入。

## 启动网页

安装 Python 3.10+，Windows 需把 Python 加入 PATH。解压整个文件夹，不要单独移动某个脚本。Windows 双击 `start.cmd`；macOS/Linux 运行 `sh start.sh`。首次安装只影响本目录的 `.venv`，缓存保存在 `.cache`。

页面通常位于 `http://127.0.0.1:8765/`，若被其他程序占用会选择后续空闲端口并自动打开。服务只监听本机；手机不能直接访问这个 localhost 网页。网页连接原生 USB 端口，其他串口工具应关闭。

关闭浏览器不会结束后台服务；可先在网页断开设备。Windows 双击 `stop.cmd` 结束本目录的后台服务；其他系统运行 `python3 tools/stop_studio.py`。再次启动会复用同一目录的服务。运行日志在 `logs/studio.log`。

## 录入和识别

1. 固定握法与方向；不要直接捏着焊脚和裸露连接处大幅挥动。可以把板子固定在绝缘支架上使用。
2. 选一个动作。自定义槽可输入名称，最多 48 个 UTF-8 字节，通常约 16 个汉字。
3. 点击“录下一份”，等三秒准备结束，拿稳到提示可以挥动，做一次动作，再停稳。
4. 保存后暂停，不会连续自动录下一次；再点击录制，补齐三份。
5. 至少训练两类明显不同的动作，然后点击“进入自动识别”。停稳、挥动、停稳，无需按 SW4。

录入与识别互斥。过小、过短、过长或传感器异常的动作会拒绝，不强行归类。识别时日常拿取也可能触发采集；误触可拒绝，但没有全场景零误触保证。

名称不会改变内部事件 ID，例如把 custom1 命名为“开灯”，事件 ID 仍是 custom1。改名不删除模板；清除类别会删除该类训练数据。

绿灯用于就绪心跳、采集与识别成功提示，红灯用于校准/拒绝等状态。具体故障看网页状态与日志，不能单凭亮灯判断整板正常。

## 样本与通信配置

按实际握法录入样本，在网页配置 Wi-Fi 和 MQTT。更换网络时更新相应配置；整片 Flash 备份可能包含凭据，不要上传 GitHub。
