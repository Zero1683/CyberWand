# 本软件包检查记录

检查日期：2026-09-15。本次检查没有重新烧录实物板，也没有更改板上的模板和无线配置。

- 在本目录新建独立 Python 3.13 虚拟环境，成功安装 pySerial 3.5、esptool 5.4.0；未依赖原 GUI 的 Python 包路径。
- 包装后的启动器避开已占用的 8765，在 8766 启动独立网页，正确返回本目录的实例标识。
- 页面与 CSS / JS / 端口接口正常；初始未连接状态正常；无令牌的控制请求返回 403。
- 停止脚本成功结束本软件目录的服务；旧开发网页未被关停。
- 七项 Python GUI 协议测试通过；前端显示、三秒到期、断线、训练模式、自定义标签测试通过。
- 四组 C++ 测试均通过：识别器、运动分段、采集时钟、静止窗口。
- BLE 16 字节示例报文解码通过，这是软件格式测试，不是新的无线实测。
- 源码通过 PlatformIO 重新编译。使用本机已有的匹配 SDK / 工具链缓存，未重新下载完整 SDK；RAM 101820 / 327680 字节，Flash 1647654 / 1966080 字节。
- `release/manifest.json` 中每个发行二进制及对象文件的 SHA-256 与文件一致。发行镜像保留原来已在板上运行的 v0.7 构建，未用这次重新构建覆盖。
- BOM 从 2026-09-11 网表导出，共 49 项；工程和 Gerber 的日期来源在硬件说明中单独标明。

Windows 已验证；macOS/Linux 的启动分支已提供，但未在这两个系统上实跑。首次依赖安装需要网络。MQTT / Home Assistant 仍需真实服务环境验证。
